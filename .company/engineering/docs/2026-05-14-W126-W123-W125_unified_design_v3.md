# 統合設計書 v3: W126 GitHub 移行 + W123 Obsidian + W124 Codex + W125 Codex Reviewer

**Date**: 2026-05-14 (v3 改訂、Phase 0 実機検証完了後)
**Author**: Claude Code (Opus 4.7)
**Status**: DRAFT v3 — code-architect review pending、user 承認待ち
**Scope**: 4 機能 W126 → W123 → W124 → W125 の **逐次** 実装、`/feature-dev` 厳格適用

**v2 → v3 主要変更** (architect review + Phase 0 検証で判明した 12 件全反映):
1. **Claude Code project hash 仕様検証完了** (path-derived `:` 除去 + `/\` → `-`、rename feasible)
2. **monitor.db 2 箇所問題解明** (data/=本物 14.9MB、root=0byte 残骸)
3. **SQLite WAL checkpoint step 追加** (`PRAGMA wal_checkpoint(TRUNCATE)`)
4. **hook 内絶対 path 4 ファイル特定** (`$CLAUDE_PROJECT_DIR` 化必須)
5. **settings.local.json の embedded path 30+ 件特定** (permission allowlist)
6. **`.gitignore` 漏れ 10+ 件特定** (既存 `tools/ebay-manager/.gitignore` を内包必須)
7. **grep 4 pattern 化** (backslash / forward slash / Git Bash / $USERPROFILE)
8. **rollback fail signal 5 件明示**
9. **success criteria を客観 metric 化**
10. **W126-α / W126-β 分割は不採用** (user 選択 = (A') 一気実行)、ただし stage 内 step 細分化
11. **W125 設計は Phase 3 (W124) Gate 後に fix** (Plus credit 実測前提)
12. **scheduler 停止時間を 3h → 6-8h 修正** (実測ファイル数 26,333 ベース)

---

## 0. 全体像

```
Phase 0 (今日中、scheduler 稼働継続)         ← pre-flight 準備、推定 4h
   ↓
Phase 1 (明日、scheduler 稼働継続)            ← Foundation Refactor (hook + settings)、推定 3h
   ↓
Phase 2 (明後日、scheduler 半日停止)          ← Migration Day、推定 6-8h
   ↓
Phase 3 (1 週間、scheduler 稼働)              ← Verification (OneDrive copy 30 日保持)
   ↓
Phase 4: W123 Obsidian (1 週間)              ← clean architecture 上
   ↓
Phase 5: W124 Codex CLI (1 週間)
   ↓
Phase 6: W125 Codex Reviewer (1 週間)
```

## 1. Goals & Non-Goals (v2 から変更なし)

### Goals
1. OneDrive 配下の実害 risk 根絶 — W126
2. memory ファイル群を Obsidian で編集・閲覧可能化 — W123
3. OpenAI Codex CLI を Claude Code から呼出可能化 — W124
4. ソースコード以外のすべてを Codex 自動 review — W125
5. 複数 AI が同一 knowledge base 参照 — W123 + W126
6. Karpathy LLM Wiki 規約の selective 採用 — Phase 0 内で着手

### Non-Goals
- 既存 memory の構造的マイグレーション (frontmatter 拡張のみ)
- Q0-Q6 / eBay 業務規約の変更
- Codex 専用 MCP server 化 (3 回出てから)
- Codex App (cloud) 採用 (CLI 単独 start)
- Pro $100/$200 即採用 (Plus $20 から実測)

---

## 2. Phase 0 検証結果 (実機データ)

### 2.1 codebase 規模

| 項目 | 実測値 |
|---|---|
| 全ファイル数 | **26,333** |
| .md | 375 |
| .py | 260 |
| .json | 644 |
| .log | 225 |
| ※ 大半は Chrome CDP profile / Terapeak profile (~20K) |

### 2.2 project hash 仕様

- 命名規則: path-derived (`:` 除去 + `\` `/` → `-`)
- 既存 hash dirs (4 個の history): `C--work-claude` / `C--work-ebay-ai-secretary` / `C--Users-gucch-OneDrive-work-claude` / 等
- **検証結論**: dir **rename で memory 引き継ぎ可能** (precedent 4 件、ただし Phase 2 で実機確認必須)

### 2.3 monitor.db 2 箇所問題

| 場所 | サイズ | 最終更新 | 状態 | 対応 |
|---|---|---|---|---|
| `tools/ebay-manager/data/monitor.db` | **14.9MB** | 5/14 11:30 active | ✅ 本物 | 移行対象 |
| `tools/ebay-manager/data/monitor.db-wal` | 1.4MB | 5/14 11:36 | ✅ active WAL | checkpoint で main DB に flush |
| `tools/ebay-manager/data/monitor.db-shm` | 32KB | 5/14 11:35 | ✅ active SHM | checkpoint で消える |
| `tools/ebay-manager/monitor.db` (root) | 0 bytes | 5/8 | 🗑 残骸 | **削除して移行** |
| `tools/ebay-manager/data/monitor.db.backup_w72_*` | 7.2MB | 5/1 | 🗑 古い backup | .gitignore + 移行非対象 |
| `tools/ebay-manager/data/backups/*.bak*` | ~30MB | 過去 | 🗑 backup | .gitignore + 移行非対象 |

### 2.4 hook 内絶対 path (要 refactor、4 ファイル)

| ファイル | 行 | ハードコード |
|---|---|---|
| `.claude/hooks/db-write-confirm.sh` | L51 | `BACKUP_DIR="C:/Users/.../data/backups"` |
| `.claude/hooks/db-preflight.sh` | L55 | `db_full="C:/Users/.../{$db}"` |
| `.claude/hooks/session-start-load-incantation.sh` | L23-24 | `ROOT_DIR` fallback + `MEM_DIR` (project hash 含む) |
| `.claude/hooks/session-start-recent.sh` | L11-37 | 3 箇所、Python pathlib 内含む |
| `.claude/commands/listing.md` | L1 | 指示文の絶対 path (情報のみ、機能影響なし) |

### 2.5 settings.local.json (Claude Code permission allowlist)

- embedded path: 30+ 件
- 主に `Bash(...)` permission の事前許可 list
- **機能影響**: 古い path 参照は migration 後 no-op (ファイル不在で no-op)、新 path は再 permission grant 必要
- 移行戦略: **古い list は touch せず、新 path で必要な permission は session 中に再付与** (user 都度承認)

### 2.6 既存 .gitignore (4 ファイル、要内包)

```
.company/secretary/learning-pipeline/.gitignore
cc-company/.gitignore
tools/ebay-manager/.gitignore  ← 主、最大
tools/ebay-manager/.pytest_cache/.gitignore  ← 自動生成
```

### 2.7 4-pattern grep 結果

| pattern | 用途 | hit ファイル数 |
|---|---|---|
| `OneDrive` | 広域 | 100+ (limit reached) |
| `C:[\\/]Users[\\/]gucch[\\/]OneDrive` | 絶対 path 直接 | 100+ |
| `gucch.OneDrive` | env var 経由 | 30+ |
| `/c/Users/gucch/OneDrive` | Git Bash 表記 | 10+ (settings.local.json 内主流) |

→ Phase 1 で **PowerShell 一括書換 + 全 pattern check** が必要

---

## 3. Architecture Decisions

### 3.1 path 構造 (新)

```
C:\Users\gucch\projects\claude\          ← project root (OneDrive 外)
├── tools\ebay-manager\
├── .company\
├── .claude\
├── scripts\
├── docs\                                ← (新規) 設計書 / README 等
├── .env                                 ← gitignored
├── .gitignore                           ← root 集約
├── CLAUDE.md
└── .git\                                ← Git 管理
```

memory dir は **既存の `C:\Users\gucch\.claude\projects\C--Users-gucch-projects-claude\memory\`** に移動 (project hash rename)

### 3.2 `.gitignore` 完全版

```gitignore
# === Secrets (絶対 commit 禁止) ===
.env
.env.local
*.key
*.pem
config/credentials.json
config/gmail_token.json
**/credentials.json
**/gmail_token.json
**/oauth_token*.json
.claude/settings.local.json              # 個人 permission allowlist (絶対 path 含む)

# === Business data ===
monitor.db
monitor.db-journal
monitor.db-wal
monitor.db-shm
tools/ebay-manager/monitor.db*           # root 残骸も除外
tools/ebay-manager/data/*.db*
tools/ebay-manager/data/monitor.log
tools/ebay-manager/data/monitor.pid
tools/ebay-manager/data/backups/         # backup ディレクトリ全体
tools/ebay-manager/data/*.json.bak*      # backup JSON
tools/ebay-manager/data/system_improvements.json.bak*

# === Browser profiles (容量大、機密) ===
tools/ebay-manager/data/.chrome_cdp_profile/
tools/ebay-manager/data/.terapeak_browser_profile/

# === Logs ===
logs/
tools/ebay-manager/logs/
*.log
tools/ebay-manager/python_install_*.log

# === Python ===
__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/
.pytest_cache/

# === Generated / large ===
models/*.onnx
data/processed_images/
tests/fixtures/design_samples/*.HEIC
tests/fixtures/design_samples/*.heic
cookies/*.txt
!cookies/.gitkeep

# === OS / IDE ===
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
Desktop.ini
*.lnk

# === OneDrive 残骸 ===
**/.OneDrive*
**/conflict_copy*

# === Temp ===
*.tmp
*.cache
node_modules/
```

### 3.3 hook refactor (Phase 1 主作業)

各 hook の修正パターン:

```bash
# Before
BACKUP_DIR="C:/Users/gucch/projects/claude/tools/ebay-manager/data/backups"

# After
BACKUP_DIR="${CLAUDE_PROJECT_DIR}/tools/ebay-manager/data/backups"
```

```bash
# Before
MEM_DIR="$HOME/.claude/projects/C--Users-gucch-OneDrive-work-claude/memory"

# After (project hash 動的計算)
PROJECT_PATH="${CLAUDE_PROJECT_DIR}"
PROJECT_HASH=$(echo "$PROJECT_PATH" | sed 's/://g' | sed 's/[\\/]/-/g')
MEM_DIR="$HOME/.claude/projects/${PROJECT_HASH}/memory"
```

### 3.4 W123-W125 設計 (v2 から変更なし、Phase 4-6 で着手)

(v2 §3.3-3.5 を継承、Phase 4 着手時に再確認)

---

## 4. Phase 別実装手順

### Phase 0: Pre-flight 準備 (今日中、scheduler 稼働継続、推定 4h)

| Step | 内容 | 工数 | 検証 |
|---|---|---|---|
| 0-1 | `.claude/rules/contradiction-annotation.md` 新設 | 30min | file exists |
| 0-2 | `.claude/rules/cascade-update.md` 新設 | 30min | file exists |
| 0-3 | `.claude/rules/wiki-frontmatter.md` 新設 | 30min | file exists |
| 0-4 | CLAUDE.md に @import 追加 | 10min | grep で確認 |
| 0-5 | robocopy `/L` dry run で総 file 数 + 容量 + 試算時間取得 | 30min | console 出力 |
| 0-6 | GitHub アカウント / repo 作成 (user 操作) | user ~5min | repo URL |
| 0-7 | `.gitignore` 最終版作成 (上記 3.2) | 30min | file exists |
| 0-8 | hook refactor の dry test (1 ファイルで `$CLAUDE_PROJECT_DIR` 動作確認) | 1h | hook execute |
| 0-9 | OneDrive offline mode 確認 (`Always keep on this device`) | 10min | OneDrive UI |
| 0-10 | backup: 全 OneDrive 内容を `C:\backup_2026-05-14\` に robocopy | 30min-2h | dir 存在 |

**user 操作 (Phase 0 内)**:
- GitHub アカウント / private repo 作成 (5min)
- OneDrive offline mode 確認 (~5min)

### Phase 1: Foundation Refactor (明日、scheduler 稼働継続、推定 3h)

| Step | 内容 | 工数 | 検証 |
|---|---|---|---|
| 1-1 | `.claude/hooks/db-write-confirm.sh` → `$CLAUDE_PROJECT_DIR` 化 | 15min | hook execute test |
| 1-2 | `.claude/hooks/db-preflight.sh` → 同上 | 15min | 同上 |
| 1-3 | `.claude/hooks/session-start-load-incantation.sh` → 同上 (project hash 動的計算) | 30min | session start 再実行 |
| 1-4 | `.claude/hooks/session-start-recent.sh` → 同上 (Python pathlib 内も) | 30min | 同上 |
| 1-5 | hook 全 4 ファイルの動作確認 (現状の OneDrive path で) | 30min | error 0 件 |
| 1-6 | grep で 4 pattern 全 hit を再 inventory (Phase 0 後の状態) | 30min | hit リスト |
| 1-7 | `.gitignore` を root に配置 | 5min | file exists |
| 1-8 | `git init` + 既存 .git 衝突確認 + 必要なら remove | 15min | .git/ 確認 |

### Phase 2: Migration Day (明後日、scheduler 半日停止、推定 6-8h)

**user 事前承認必要** (scheduler 半日停止 + 業務 critical タイミング避ける)

| Step | 内容 | 工数 | 検証 / Fail signal |
|---|---|---|---|
| 2-1 | scheduler 停止 (`Stop-Process` PID 38996) | 5min | 最終 log 行 |
| 2-2 | `sqlite3 data/monitor.db "PRAGMA wal_checkpoint(TRUNCATE);"` | 5min | -wal/-shm が 0 bytes |
| 2-3 | `sqlite3 data/monitor.db "PRAGMA integrity_check;"` | 5min | `ok` 出力 |
| 2-4 | root の `tools/ebay-manager/monitor.db` (0byte 残骸) 削除 | 1min | gone |
| 2-5 | `robocopy /MIR /COPYALL /R:3 /W:5` で OneDrive → 新 path | 2-4h | 完了 + exit code 0-7 |
| 2-6 | grep で全 4 pattern 一括書換 (PowerShell sed-like) | 30min | hit count 0 確認 |
| 2-7 | `.claude/projects/` 内の hash dir rename: `C--Users-gucch-OneDrive-work-claude` → `C--Users-gucch-projects-claude` | 5min | dir exists |
| 2-8 | `cd C:\Users\gucch\projects\claude\` + `git status` で 未 tracked file 確認 | 15min | size < 1GB |
| 2-9 | `git add .` + `git commit -m "Initial migration from OneDrive"` | 15min | commit hash |
| 2-10 | `git remote add origin <user repo URL>` + `git push -u origin main` | 15min | push 成功 |
| 2-11 | scheduler 起動 (新 path から `pythonw daily_scheduler.py`) | 5min | log 起動行 |
| 2-12 | 5 fail signal 確認 (下記 §6) | 30min | 全部 pass |
| 2-13 | Discord 通知で「migration 完了」report | 10min | webhook 動作 |

#### 5 fail signal (architect 要求、Phase 2 step 2-12)

1. **scheduler.log に `ERROR` / `Traceback` 出現** → 即 rollback
2. **`tasks_enabled` cron 未登録** (起動 5 分後 check) → 即 rollback
3. **`monitor.db` 新規 write なし** (起動 30 分後、`task_execution_log` の latest row が古い) → 即 rollback
4. **hook が起動毎 BLOCK** (path 誤参照で発火失敗) → 即 rollback
5. **Discord 通知未着** (health_alert_log) → 即 rollback

### Phase 3: Verification (1 週間、scheduler 稼働、OneDrive copy 保持)

| 内容 | metric |
|---|---|
| 日次 smoke test | `task_execution_log` で W122/W183/daily_relist 全部 success |
| monitor.db 行数 monitor | 7 日前比で減少していない (= 既存データ保持) |
| GitHub push 自動化 (任意) | 日次 commit, 7 日で commit ≥ 7 件 |
| Discord health_alert_log monitor | 7 日間 critical 0 件 |
| OneDrive copy: 30 日保持 | rollback の生命線 |

**Phase 3 終了条件**: 7 日連続異常なし → Phase 4 (W123) 着手承認

### Phase 4-6: W123/W124/W125 (各 1 週間、v2 §4 を継承)

clean architecture 上で実行、詳細は v2 §4 (Phase 2-4) 参照。**Phase 4 着手前に再度 Phase 3 終了確認** が necessary gate。

---

## 5. Rollback Plan

### Phase 2 移行中の rollback (30 分以内判断)

1. 新 scheduler 停止
2. 新 path 削除 (`Remove-Item -Recurse C:\Users\gucch\projects\claude\`)
3. OneDrive 内 `.claude/projects/C--Users-gucch-OneDrive-work-claude` を hash rename した場合 revert
4. OneDrive 内 project から scheduler 再起動
5. monitor.db -wal/-shm が migration step 2-2 の checkpoint で消えている場合、データ保存性確認 (checkpoint で main DB に flush 済なので問題なし)
6. GitHub repo は public 化されていない限り削除可能

### Phase 2 完了後 〜 Phase 3 中の rollback (1-30 日以内)

1. scheduler 停止
2. **OneDrive copy 復活**: `robocopy C:\backup_2026-05-14\ → 元 path`
3. scheduler を OneDrive path から再起動
4. GitHub repo は archive (削除しない、history 保持)

### Phase 3 終了 (7 日異常なし) 後の cleanup (Month 2)

1. OneDrive 内 project フォルダを **別 drive に archive** (3 ヶ月後完全削除候補)
2. `C:\backup_2026-05-14\` の cold backup は半年保持

---

## 6. Success Criteria (K3 客観 metric)

### Phase 0
- 新 3 rule + CLAUDE.md @import 反映
- robocopy /L で総 file + 容量 + 試算時間 user 提示
- hook refactor dry test pass

### Phase 1
- 4 hook 全部 `$CLAUDE_PROJECT_DIR` 化
- hook 実行で error 0 件 (OneDrive path 状態でも動く)

### Phase 2 (5 fail signal § 4 step 2-12)
1. scheduler 起動 30 分後 ERROR / Traceback = 0 件
2. cron 全項目登録確認 (`grep "cron\[" scheduler.log`)
3. monitor.db に新規 row 1 件以上
4. hook BLOCK = 0 件
5. Discord 通知 1 件以上到達

### Phase 3 (7 日 verification)
- W122/W183/daily_relist 全部 7 日連続 success
- monitor.db 行数: 移行前 = X、7 日後 = X + Y (Y > 0、= 業務継続)
- Discord critical = 0 件
- conflict copy = 0 件 (新 path に発生しないことは新 path が OneDrive 外なので保証されるが、OneDrive 側残骸を確認)

### Phase 4-6 (v2 継承)

---

## 7. Risks & Mitigations (v2 から拡張)

| Phase | Risk | 影響 | Mitigation |
|---|---|---|---|
| 0 | OneDrive offline 化忘れ → robocopy が hang | 移行時間倍増 | Phase 0 step 0-9 で必須確認 |
| 1 | hook refactor で動作不全 | session start 不能 | 各 hook の単体テスト必須、failover 行 (= old path) を残す |
| 2 | robocopy が想定以上の時間 (26K files) | 停止時間延長 | Phase 0 dry run で実測、見積もり +50% buffer |
| 2 | WAL flush 漏れで monitor.db corruption | 売上 / 在庫データ喪失 | step 2-2/2-3 で必須実行、step 2-3 で `ok` 確認 |
| 2 | project hash rename が想定外動作 | memory orphan | Phase 2 で実機検証、rename 前 backup |
| 2 | grep 一括書換漏れ | scheduler 動作不全 | 4 pattern 全部実行、step 2-6 で hit count 0 確認 |
| 2 | git push reject (容量 / secret 検出) | push 失敗 | step 2-8 で size 確認、`.gitignore` 厳格、step 2-9 dry run |
| 2 | scheduler 再起動失敗 | W122 / W183 cron 死 | rollback 30 分以内判断、Phase 0 で OneDrive backup 必須 |
| 3 | 1 週間中の業務影響 | active listing 監視欠落 | 日次 smoke test、health_alert_log monitor |
| 4-6 | (v2 継承) | | |

---

## 8. user 操作 inventory

### Phase 0 (今日)
- GitHub アカウント / private repo 作成 (5min)
- OneDrive offline mode 確認 (5min)

### Phase 2 (移行日)
- scheduler 半日停止承認 (タイミング決定)
- migration 完了後の動作確認 (30min、user 主観の業務感覚で OK 判断)

### Phase 4-6 (v2 継承)
- Obsidian インストール (W123)
- ChatGPT Plus 加入 (W124、既存なら不要)

---

## 9. 改訂履歴

- 2026-05-14 v3 (Claude Opus 4.7) — Phase 0 実機検証 + architect review 12 件全反映
- 2026-05-14 v2 — W126 を foundation に追加、architect review で 12 件指摘
- 2026-05-14 v1 — W123-W125 単独、architect review で抜本再考
