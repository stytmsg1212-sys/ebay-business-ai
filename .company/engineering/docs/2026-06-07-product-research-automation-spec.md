# 商品リサーチ→仕入判定→出品 自動化 仕様書（決定ロック）

**作成**: 2026-06-07 / **状態**: 仕様確定（実装未着手） / **モデル**: Opus 4.8 + Codex(GPT-5.5)協議

user とのチャットで段階的に詰めた、コンサル(けいすけ)流リサーチ手法の自動化仕様。
本書がロック済み決定の正本。実装時はこれを参照。

---

## 0. 全体像（2フェーズ）

| フェーズ | 内容 | 自動化の難易度 |
|---|---|---|
| **A. 発掘** | Terapeak Product Research を除外KW/カテゴリ/$100+/JP seller でハーベスト→候補化。**収穫は2パターン** (2026-06-10 user 確定: ①Last 7days+新着順→直近24h窓 / ②Last 2years+古い順→2年前当日24h窓、詳細=設計書 2026-06-10 §5-0) | 🔴 重い・規約/anti-botリスク（後回し） |
| **B. 検証〜出品** | 候補ごとに 売れ行き判定→フリマ仕入探索→利益判定→在庫あり/在庫0/skip→人間承認→出品/監視 | 🟢 既存資産8割流用（MVP） |

**MVP方針**: フェーズAは当面**手動**（user がTerapeakで発掘し商品名を貼る）、**フェーズBを半自動Wizard化**。AのスクレイプはPoCで安全性実証後に判断。

---

## 1. データ入手性（一次情報で確認、2026-06）

- **Terapeak Product Research**: 過去3年sold・無料・**公式API無し（UI専用）**。出品(active)側に**出品開始年月**も表示される（competitor listing age 取得可）。1〜2年遡及は可能。sell-throughは90日検索時の指標。
- **Marketplace Insights API**（唯一の公式sold API）: Limited Release＝**本番認可困難・90日のみ** → 発掘の公式API代替は非現実的。
- **Browse API**: アクティブ商品のみ（売却済不可）、5,000 calls/日。現在competitor有無の確認に利用可。
- 「250件/日」は公式API資料に無く、Terapeak **UI側の実運用制限**として要実測。

---

## 2. フェーズB 確定仕様

### 2-A. 売れ行きゲート（どの商品を出品候補にするか）

判定材料（Terapeak）: 直近90日sold数 / 1〜2年sold数 / 出品の有無・各出品の開始年月・売れているか

| 優先 | 条件 | 判定 |
|---|---|---|
| 1 | 直近90日に **2個以上** | ✅ 対象（在庫あり寄り） |
| 2 | 90日<2 ＋ 出品あり ＋ **開始90日以上**で売れてない | ❌ 除外（死に筋） |
| 3 | 90日<2 ＋ 出品あり ＋ **開始90日未満**で売れてない | ⏭ 今回スキップ（保留せず、再出現時に再判定） |
| 4 | 90日<2 ＋ **出品ゼロ** ＋ 1〜2年で **2個以上** | ✅ 対象（在庫0＋監視） |
| 5 | 1〜2年でも **2個未満**（過去も需要なし） | ❌ 除外 |

- 競合が90日以上出してて売れない＝死に筋（過去需要があっても除外）。
- 過去需要の救済（行4）は「今 競合ゼロ」のときだけ。
- view/watchは競合分が機械取得困難なため「出品あり＋直近売れてない（sell-through≈0）」で代替判定。

### 2-B. 仕入れ探索（手順12-13）

- **フリマのみ**（メルカリ／ヤフオク／PayPay）で同一商品＋同状態を探す。**Amazon・楽天等ECは対象外。**
- 取得: 今の最安値 ＋ 過去取引履歴（メルカリ売り切れ／ヤフオク落札相場）= "安く出たら買える"根拠。
- **同一商品＋同状態の最終一致判定は人間が確認**（AIは候補＋match_score/確信度を提示するまで。状態誤判定＝返品/Defect事故防止）。
  - 2026-06-11 追補: 自動経路 (W228 Phase 3 / task_research_sourcing) では match_score < 60 を task 側で not_found 降格＋利益値クリア（supplier-matching-rules 準拠。本項の「人間が確認」は手動 UI 前提で、無人バッチが score 不問の sourced を承認待ちに積むと誤マッチ利益が並ぶ — 2026-06-11 初回実機 15/15 件事故で発覚）。

### 2-C. 利益計算＆仕分け（手順14）

- 利益 ＝ eBay見込み売値（Terapeak平均）−（仕入値＋国内送料＋国際送料＋関税＋eBay手数料）
- **関税還付は含めない（保守計算）**。
- 利益基準: **けいすけ基準＝利益率6% または 利益額600円（どちらか片方クリアでOK）**。撒き餌商品は別扱い。

| 今 見つかる？ | 今の価格で基準クリア | 過去価格なら基準クリア | アクション |
|---|---|---|---|
| 見つかる | ○ | — | ✅ 在庫あり出品 |
| 見つかる | × | ○ | ✅ 在庫0＋監視 |
| 見つかる | × | × | ❌ スキップ |
| 見つからない | — | ○（過去取引あり） | ✅ 在庫0＋監視 |
| 見つからない | — | ×／過去取引なし | ❌ スキップ |

### 2-D. 出品/監視連携（承認後）

- 在庫あり → draft/verify 経由で出品（W226 description生成流用）。
- 在庫0 → 出品＋**キーワード新着監視を自動登録**（安く出たら検知→購入→在庫1）。
- **仕入購入と初回出品公開は人間承認**（完全自動購入はしない）。

---

## 3. 既存資産マッピング（再利用）

| 工程 | 既存資産 | 再利用度 |
|---|---|---|
| 売れ行き判定 | `monitor/terapeak_scraper.py`（集計のみ。出品開始日・結果リスト読取は要追加） | 🟡 拡張 |
| フリマ仕入探索 | `tasks/task_supplier_candidate_search.py`（メルカリ/ヤフオク/PayPay検索+在庫+評価+利益+候補登録） | 🟢 ほぼ完成 |
| 同一性/状態判定 | `monitor/claude_evaluator.py`（match_score 80/60閾値） | 🟢 |
| 利益計算 | `monitor/lowest_price.py`（compute_breakeven_price_usd、4区分、還付抜き=現状と整合） | 🟢 |
| キーワード監視 | `monitor/keyword_watch_db.py`（W148/W206。出品→watch自動紐付けのみ未実装、W206 ebay_item_idで容易） | 🟡 |
| 在庫0出品 | `monitor/ebay_lister.py`（在庫0可）+ W226 description生成 | 🟢 |

**必要な接着剤**: `research_candidates` 新テーブル（Terapeak発掘候補を仮listingとして既存B系に流す）+ Wizard UI。

---

## 4. コスト試算

- **Claude API**: Sonnet $3/$15 per MTok（Batch半額）。評価 約$0.015-0.03/候補。100候補≈$1.5-3、500候補≈$7.5-15。重いのはAPIでなくPlaywright実行時間+再試行。
- **開発工数**（Codex試算・私同意）: 小さく作って4-6週、業務運用レベル6-10週。完全自動購入はさらに増。
- **レート**: Browse/Insights 5,000/日。Terapeak UIは未実証の実運用上限（アカウントリスク）。

---

## 5. 最大の落とし穴（事故防止）

1. **「売れている」≠「同状態で確実に仕入・履行できる」** の取り違え（最重要）。
2. 在庫0出品の**履行不能**（=Defect=アカウント停止。MonoHonpoの最優先リスク）。
3. Terapeak UIスクレイプの規約/アカウントリスク（フェーズA）。
4. AI同一性/状態判定の過信 → **人間承認で担保**。

---

## 6. 段階導入（ROADMAP）

| W | 内容 | 規模 |
|---|---|---|
| **W228** | フェーズB半自動パイプライン（商品名貼付→売れ行きゲート→フリマ仕入探索→利益判定→仕分け→人間承認→出品/監視）。research_candidates基盤+Wizard。 | 4-6週 |
| **W229** | フェーズA Terapeak Product Research ハーベスト自動化 PoC（規約・anti-bot・出品開始日読取 実証）。収穫2パターン=設計書 2026-06-10 §5-0。 | 1-2週(PoC) |

Q3構造化フローで実装（設計→code-reviewer+Codex 2段→Q1検証）。

---

## 7. 確定した決定（ロック・チャット由来 2026-06-07）

1. 売れ行きゲート5分岐（上記2-A）。境目90日。保留はせず再出現時再判定。
2. 仕入探索はフリマ(メルカリ/ヤフオク/PayPay)のみ、Amazon/楽天EC対象外。
3. 同一商品＋同状態の最終一致は人間確認（AIは候補提示）。
4. 利益基準=けいすけ基準（率6% **または** 額600円、どちらか片方）、**還付抜き**。
5. 過去取引参照＝フリマの売却済み/落札相場で「次に安く出れば仕入れて利益」と判断。
6. 仕入購入・初回出品公開は人間承認（完全自動購入なし）。
7. MVP=フェーズB半自動、フェーズA発掘は当面手動（PoC後判断）。

---

## 8. レビュー反映（2026-06-07 code-architect設計レビュー）

初版の「既存資産8割流用」は**楽観だった**。実コードと照合した結果、実装前に潰すべき構造的問題が判明。確信度高い順:

### P0（実装前必須）
- **P0-1 「8割流用」は過大 → アダプタ層が必要**。既存 `task_supplier_candidate_search` は「eBay listing存在前提」設計で、Terapeak由来の未出品候補は3つの不変条件に衝突:
  - エントリが `get_ebay_listing_by_item_id` で既存listing必須（未出品候補は引けない）
  - `add_supplier_candidate` は `ebay_item_id` NOT NULL（W185 v56・SKU崩壊事故防止）→ **NULL禁止**
  - 利益計算は `current_price`(既存eBay売値)+物理データ(weight)前提 → Terapeak候補は「Terapeak平均」しか無く weight 無し → 計算不能
  - **対策**: `research_candidates` に**独自PK**（例 `rc:<id>`）を持たせ、既存テーブルの ebay_item_id には sentinel（`rc:<id>`、NULL禁止）を入れ、実出品後に実IDへ昇格。利益計算は既存関数でなく**Terapeak平均+人手/推定の物理データを直接 compute_breakeven に渡す新関数**。
- **P0-2 ステータス状態機械が未定義** → silent skip温床。`new→gate_passed/rejected/skipped→sourcing→sourced/not_found→awaiting_identity_approval→…→listed/listed_oos_monitored / needs_review` を明示定義。「ゲートskip」は理由+日時を残す(再出現再判定のため)が候補リストには非表示。
- **P0-3 在庫0×自動監視→購入 が履行不能を増幅** → 在庫0アクティブ**上限件数**設定、handling time最大化、「承認ラグ中にeBayで売れた時の緊急フロー(即取下げ/高速発注)」を必須化。

### P1（MVPの正しさ・コスト）
- **P1-1 利益計算の物理データ欠落** → Terapeakには weight/HS無し。MVPは**人手で概算重量/サイズ入力**。Section232該当(鋳鉄/トランス/家電)は赤字リスク大 → ゲートでHS推定→該当フラグを人間に提示。weight=0 clip常用は禁止(送料過小→偽黒字→誤仕入)。
- **P1-2 ⚠️フェーズBのゲートが実はフェーズAのscrapeに依存** → ゲート行2/3/4の判定材料(出品有無・**出品開始年月**・1〜2年sold)はTerapeak ACTIVEタブ/長期SOLD読取=フェーズA相当。現 terapeak_scraper はSOLDタブ集計特化でACTIVE開始日は未対応(=新規開発、W229相当)。**「Bだけ先行」は不成立**。→ **MVPはゲート入力を人手入力**(商品名+90日sold数+出品有無+開始日+1-2年sold数を貼る)。scraper自動取得はW229へ。
- **P1-3 CDP手動ログイン前提はループ運用に乗らない** → MVPでscraperをループに組まない(人手入力前提)。

### P2/P3（Q0・検証・ビルド順）
- **P2 「スキップ」を業務判断(死に筋/赤字)と技術失敗(取得エラー/計算不能)で別状態化**。後者は needs_review + DBログ/Discord(silent skip禁止)。
- **最初のPoC** = 「1商品 手入力 → フリマ探索 + AI同一性提示まで(出品しない)」。DoD=実フリマ3商品でmatch_scoreがresearch_candidatesに着地・利益計算がNoneにならない(orneeds_reviewに正しく落ちる)。
- **ビルド順(改訂)**: ①research_candidatesテーブル+状態機械(識別子設計) → ②Terapeak候補→フリマ探索アダプタ(=8割流用の真偽を2番目で検証) → ③売れ行きゲート(人手入力前提・純ロジックpytest) → ④利益計算&仕分け → ⑤承認UI(同一性→購入→公開3段) → ⑥出品+監視自動登録(最も金銭直結=最後)。最大リスク(アダプタ)を前倒し。
- **コスト注記**: 「100候補≈$1.5-3」はW223のコスト削減(再評価skip)がebay_item_idキー前提で効く想定の楽観値。Terapeak候補では効かない場合あり。

### 結論
ビジネスロジック(ゲート/けいすけ基準/人間承認)は健全。だが**MVPスコープは初版より重い**(アダプタ新規+状態機械+ゲートは当面人手入力)。W228着手時は P0-1/P0-2/P1-2 を設計に反映し、P2-2のPoC(出品せず探索+同一性提示)を最初の検証単位にする。

---

## 9. 関連リサーチ手法（research-brain 提案、2026-06-07・ROADMAP候補）

本パイプラインと別に検討価値が高い手法（優先度順）:
- **⭐S 自社sold縦深掘り**（ROI極大/コスト小）: 売れたlistingの色違い/上位機/兄弟SKU/後継型番を自動列挙。W122 morning_discovery が既に近いことをするが**実sold実績をFew-shotに入れていない** → sold履歴を注入するだけ。最小コストで最大ROI。
- **A ライバル新規出品モニター**（user提案・ROI大/コスト中）: eBay Browse APIの `sellers:{id1|id2}` フィルタ(正式サポート)で、**JP優良セラー5-15名**を登録→新規出品を差分検知→claude_evaluatorで一次フィルタ→morning_discovery階層3へ供給。W153(自社listing別競合)/W148(仕入新着)と**重複ゼロ=自社未踏ジャンル発掘の唯一手**。JP判定ゲート必須・人選は人間・学習目的(自動狙い撃ち値下げしない)。80%既存流用。
- **A Terapeak sold × JP競合ゼロ 自動スクリーニング**（ROI大/コスト中）: フェーズBゲートの機械化。競合ゼロ=価格決定権=利益率6%超えやすい。Terapeak 250件/日制限で夜間直列バッチ必須。
- **B 仕入先FB学習**（ROI中〜大/要意図確認）: user の buy/skip 判定を教師に claude_evaluator しきい値較正(W223延長)。
- **C 廃番・限定先回り**（ROI中）: W209ニュースAI基盤に「生産終了/限定」軸追加。
- **避ける**: Amazon/楽天仕入(false-OOS→Defect)、アニメ/IP(VeRO/Schedule A訴訟→自動採用せず人間承認)。
